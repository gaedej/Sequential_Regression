# Global Comments More
# Set Variables
x <- c(8:15,c(50:1))
y <- c(7,4,8,4,1,11,4,9,c(1:50))
z <- c(y[1],y[2],y[3])
counter = 1
counter2 = 1
my_correlation = NULL

# Start Nested Loops
for(i in boo){
  cat(i,"   " )
   for(i2 in hoo){ 
     cat(i2,"   ")
  }
   cat("**********")
  }
  